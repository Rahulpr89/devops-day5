name: Deploy Static Site to ECS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Login to Amazon ECR
      run: aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 832721971180.dkr.ecr.us-east-1.amazonaws.com

    - name: Build Docker image
      run: docker build -t my-web-ecr .

    - name: Tag Docker image
      run: docker tag my-web-ecr:latest 832721971180.dkr.ecr.us-east-1.amazonaws.com/my-web-ecr:latest

    - name: Push Docker image to ECR
      run: docker push 832721971180.dkr.ecr.us-east-1.amazonaws.com/my-web-ecr:latest

    - name: Register new task definition
      run: |
        aws ecs register-task-definition \
          --cli-input-json file://ecs-task-def.json

    - name: Update ECS service with new task definition
      run: |
        aws ecs update-service \
          --cluster devops-cluster \
          --service my-app-tast-service-zxjou1x0 \
          --task-definition my-app-tast
